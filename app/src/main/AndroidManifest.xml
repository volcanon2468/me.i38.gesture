<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" />
    <uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.SET_VOLUME_KEY_LONG_PRESS_LISTENER" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <uses-permission android:name="android.permission.REORDER_TASKS" />

    <application
        android:name=".GestureApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MicroGesture"
        android:requestLegacyExternalStorage="true"
        tools:targetApi="31">

        <!-- Main Activity -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.MicroGesture">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Settings Activity -->
        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity" />

        <!-- Gesture Edit Activity -->
        <activity
            android:name=".ui.gesture.GestureEditActivity"
            android:exported="false"
            android:parentActivityName=".ui.settings.SettingsActivity" />

        <!-- App Custom Activity -->
        <activity
            android:name=".ui.apps.AppCustomActivity"
            android:exported="false"
            android:parentActivityName=".ui.settings.SettingsActivity" />

        <!-- Robot Config Activity -->
        <activity
            android:name=".ui.automation.RobotConfigActivity"
            android:exported="false"
            android:parentActivityName=".ui.settings.SettingsActivity" />

        <!-- Panel Edit Activity -->
        <activity
            android:name=".ui.panel.PanelEditActivity"
            android:exported="false"
            android:parentActivityName=".ui.settings.SettingsActivity" />

        <!-- Lab Activity -->
        <activity
            android:name=".ui.lab.LabActivity"
            android:exported="false"
            android:parentActivityName=".ui.settings.SettingsActivity" />

        <!-- FAQ Activity -->
        <activity
            android:name=".ui.faq.FaqActivity"
            android:exported="false"
            android:parentActivityName=".ui.settings.SettingsActivity" />

        <!-- Select Action Activity -->
        <activity
            android:name=".ui.selection.SelectActionActivity"
            android:exported="true"
            android:parentActivityName=".ui.gesture.GestureEditActivity">
            <intent-filter>
                <action android:name="android.intent.action.CREATE_SHORTCUT" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Shortcut Activity -->
        <activity
            android:name=".ui.shortcut.ShortcutActivity"
            android:exported="true"
            android:theme="@style/Theme.Transparent"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance" />

        <!-- Proxy Activity -->
        <activity
            android:name=".ui.proxy.ProxyActivity"
            android:exported="true"
            android:theme="@style/Theme.Transparent"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance" />

        <!-- IME Activity -->
        <activity
            android:name=".ui.ime.IMEActivity"
            android:exported="true"
            android:theme="@style/Theme.Transparent"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance" />

        <!-- Accessibility Services -->
        <service
            android:name=".service.GestureAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Gesture Extra Service -->
        <service
            android:name=".service.GestureExtraService"
            android:exported="true"
            android:foregroundServiceType="mediaProjection" />

        <!-- Quick Settings Tile -->
        <service
            android:name=".service.QuickSettingsTileService"
            android:exported="true"
            android:icon="@drawable/ic_gesture"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Beautify Services -->
        <service
            android:name=".service.BeautifyActionService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.x1y9.beautify" />
            </intent-filter>
            <meta-data
                android:name="meta"
                android:value="@string/beautify_actions_meta" />
        </service>

        <service
            android:name=".service.BeautifySwitchService"
            android:exported="true" />

    </application>
</manifest>